<h1>編集画面</h1>
<%= form_with model: @airticle, url: airticle_path(@airticle.id), method: :patch do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :title, value: @airticle.title %>

  <h4>本文</h4>
  <%= f.text_area :body, value: @airticle.body %>

  <%= f.submit '保存' %>

<% end %>